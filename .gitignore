bin
.DS_Store

.env.prod
.env.stage
.env.dev
.env.dev.secrets
.env.stage.secrets
.env.prod.secrets
.env.*.secrets
.env.*.local

*.log

node_modules

Chart.lock
**/*.tgz

go.work.sum

.data

.turbo
frontend/out


# contentlayer
.contentlayer

/package.json
/package-lock.json

env/

tmp
venv/
venv2/
python/dist/
__pycache__/

husonym_ee_ca.key
husonym_cloud_ca.key

.env.app.local
.env.api.local
.env.worker.local

# License signing material and the registry of issued licenses. These live outside the
# repository (~/.husonym/ee-signing by default); the patterns are a backstop against a
# stray copy landing here. The private key cannot be replaced and the registry holds
# customer names and live licenses.
*_ca.key
registry.json
ee_license
