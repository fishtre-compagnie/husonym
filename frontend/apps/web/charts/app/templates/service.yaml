apiVersion: v1
kind: Service
metadata:
  name: {{ template "husonym-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "husonym-app.fullname" . }}
spec:
  ports:
    - name: http
      port: {{ .Values.servicePort }}
      protocol: TCP
      targetPort: {{ .Values.containerPort }}
  selector:
    app: {{ template "husonym-app.fullname" . }}
