apiVersion: v1
kind: Secret
metadata:
    name: {{ template "husonym-app.fullname" . }}-service-evs-{{ include "husonym-app.env-vars" . | sha256sum | trunc 8 }}
    namespace: {{ .Release.Namespace }}
    annotations:
        checksum/stringdata: {{ include "husonym-app.env-vars" . | sha256sum }}
type: Opaque
stringData:
{{ include "husonym-app.env-vars" . | indent 4 }}
