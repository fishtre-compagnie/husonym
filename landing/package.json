{
  "name": "husonym-landing",
  "private": true,
  "version": "0.0.0",
  "description": "Marketing site for www.husonym.com",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check-locales",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  },
  "engines": {
    "node": ">=20"
  }
}
