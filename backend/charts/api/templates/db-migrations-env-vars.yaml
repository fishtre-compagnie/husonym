apiVersion: v1
kind: Secret
metadata:
    name: {{ template "husonym-api.fullname" . }}-migration-evs-{{ include "husonym-api.migration-env-vars" . | sha256sum | trunc 8 }}
    namespace: {{ .Release.Namespace }}
    annotations:
        checksum/stringdata: {{ include "husonym-api.migration-env-vars" . | sha256sum }}
type: Opaque
stringData:
{{ include "husonym-api.migration-env-vars" . | indent 4 }}
