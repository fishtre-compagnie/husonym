apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  namespace: {{ .Release.Namespace }}
  name: {{ template "husonym-api.fullname" . }}-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ template "husonym-api.fullname" . }}-role
subjects:
  - kind: ServiceAccount
    name: {{ include "husonym-api.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
