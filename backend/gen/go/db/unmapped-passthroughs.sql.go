// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: unmapped-passthroughs.sql

package db_queries

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deleteUnmappedPassthroughsNotSeenInRun = `-- name: DeleteUnmappedPassthroughsNotSeenInRun :exec
DELETE FROM husonym_api.unmapped_passthroughs
WHERE job_id = $1
  AND account_id = $2
  AND last_seen_job_run_id <> $3::text
`

type DeleteUnmappedPassthroughsNotSeenInRunParams struct {
	JobId     pgtype.UUID
	AccountId pgtype.UUID
	JobRunId  string
}

func (q *Queries) DeleteUnmappedPassthroughsNotSeenInRun(ctx context.Context, db DBTX, arg DeleteUnmappedPassthroughsNotSeenInRunParams) error {
	_, err := db.Exec(ctx, deleteUnmappedPassthroughsNotSeenInRun, arg.JobId, arg.AccountId, arg.JobRunId)
	return err
}

const getUnmappedPassthrough = `-- name: GetUnmappedPassthrough :one
SELECT account_id, job_id, table_schema, table_name, column_name, data_type, first_seen_at, last_seen_at, last_seen_job_run_id FROM husonym_api.unmapped_passthroughs
WHERE job_id = $1
  AND account_id = $2
  AND table_schema = $3
  AND table_name = $4
  AND column_name = $5
`

type GetUnmappedPassthroughParams struct {
	JobId       pgtype.UUID
	AccountId   pgtype.UUID
	TableSchema string
	TableName   string
	ColumnName  string
}

func (q *Queries) GetUnmappedPassthrough(ctx context.Context, db DBTX, arg GetUnmappedPassthroughParams) (HusonymApiUnmappedPassthrough, error) {
	row := db.QueryRow(ctx, getUnmappedPassthrough,
		arg.JobId,
		arg.AccountId,
		arg.TableSchema,
		arg.TableName,
		arg.ColumnName,
	)
	var i HusonymApiUnmappedPassthrough
	err := row.Scan(
		&i.AccountID,
		&i.JobID,
		&i.TableSchema,
		&i.TableName,
		&i.ColumnName,
		&i.DataType,
		&i.FirstSeenAt,
		&i.LastSeenAt,
		&i.LastSeenJobRunID,
	)
	return i, err
}

const getUnmappedPassthroughsByAccount = `-- name: GetUnmappedPassthroughsByAccount :many
SELECT account_id, job_id, table_schema, table_name, column_name, data_type, first_seen_at, last_seen_at, last_seen_job_run_id FROM husonym_api.unmapped_passthroughs
WHERE account_id = $1
ORDER BY job_id, table_schema, table_name, column_name
`

func (q *Queries) GetUnmappedPassthroughsByAccount(ctx context.Context, db DBTX, accountid pgtype.UUID) ([]HusonymApiUnmappedPassthrough, error) {
	rows, err := db.Query(ctx, getUnmappedPassthroughsByAccount, accountid)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []HusonymApiUnmappedPassthrough
	for rows.Next() {
		var i HusonymApiUnmappedPassthrough
		if err := rows.Scan(
			&i.AccountID,
			&i.JobID,
			&i.TableSchema,
			&i.TableName,
			&i.ColumnName,
			&i.DataType,
			&i.FirstSeenAt,
			&i.LastSeenAt,
			&i.LastSeenJobRunID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getUnmappedPassthroughsByJob = `-- name: GetUnmappedPassthroughsByJob :many
SELECT account_id, job_id, table_schema, table_name, column_name, data_type, first_seen_at, last_seen_at, last_seen_job_run_id FROM husonym_api.unmapped_passthroughs
WHERE job_id = $1
  AND account_id = $2
ORDER BY table_schema, table_name, column_name
`

type GetUnmappedPassthroughsByJobParams struct {
	JobId     pgtype.UUID
	AccountId pgtype.UUID
}

func (q *Queries) GetUnmappedPassthroughsByJob(ctx context.Context, db DBTX, arg GetUnmappedPassthroughsByJobParams) ([]HusonymApiUnmappedPassthrough, error) {
	rows, err := db.Query(ctx, getUnmappedPassthroughsByJob, arg.JobId, arg.AccountId)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []HusonymApiUnmappedPassthrough
	for rows.Next() {
		var i HusonymApiUnmappedPassthrough
		if err := rows.Scan(
			&i.AccountID,
			&i.JobID,
			&i.TableSchema,
			&i.TableName,
			&i.ColumnName,
			&i.DataType,
			&i.FirstSeenAt,
			&i.LastSeenAt,
			&i.LastSeenJobRunID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertUnmappedPassthrough = `-- name: UpsertUnmappedPassthrough :exec
INSERT INTO husonym_api.unmapped_passthroughs (
    account_id,
    job_id,
    table_schema,
    table_name,
    column_name,
    data_type,
    last_seen_job_run_id
)
VALUES (
    $1,  -- account_id
    $2,  -- job_id
    $3,  -- table_schema
    $4,  -- table_name
    $5,  -- column_name
    $6,  -- data_type
    $7   -- last_seen_job_run_id
)
ON CONFLICT (job_id, table_schema, table_name, column_name)
DO UPDATE SET
    data_type = EXCLUDED.data_type,
    last_seen_at = CURRENT_TIMESTAMP,
    last_seen_job_run_id = EXCLUDED.last_seen_job_run_id
`

type UpsertUnmappedPassthroughParams struct {
	AccountID        pgtype.UUID
	JobID            pgtype.UUID
	TableSchema      string
	TableName        string
	ColumnName       string
	DataType         string
	LastSeenJobRunID string
}

func (q *Queries) UpsertUnmappedPassthrough(ctx context.Context, db DBTX, arg UpsertUnmappedPassthroughParams) error {
	_, err := db.Exec(ctx, upsertUnmappedPassthrough,
		arg.AccountID,
		arg.JobID,
		arg.TableSchema,
		arg.TableName,
		arg.ColumnName,
		arg.DataType,
		arg.LastSeenJobRunID,
	)
	return err
}
