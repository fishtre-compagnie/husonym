apiVersion: v1
kind: Secret
metadata:
    name: {{ template "husonym-worker.fullname" . }}-service-evs-{{ include "husonym-worker.env-vars" . | sha256sum | trunc 8 }}
    namespace: {{ .Release.Namespace }}
    annotations:
        checksum/stringdata: {{ include "husonym-worker.env-vars" . | sha256sum }}
type: Opaque
stringData:
{{ include "husonym-worker.env-vars" . | indent 4 }}
